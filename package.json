{
  "name": "nasa",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install": "npm install --prefix client & npm start --prefix server",
    "server": "npm start --prefix server",
    "client": "npm start --prefix client",
    "start": "npm run server & npm run client",
    "deploy": "npm run client --prefix client & npm run server --prefix server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mongoose": "^7.1.1"
  }
}
